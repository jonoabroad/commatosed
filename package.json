{
  "name": "commatosed",
  "version": "1.0.0",
  "description": "csv parser for elm",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "$TRAVIS_BUILD_DIR/sysconfcpus/bin/sysconfcpus --num 1 elm-test"
  },
  "devDependencies": {
    "elm": "latest",
    "elm-test": "beta"
  }
}